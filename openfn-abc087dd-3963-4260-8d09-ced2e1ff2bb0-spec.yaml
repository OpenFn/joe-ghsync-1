name: joe-ghsync-v1
description: |
  Testing V1 GH sync

collections: null
credentials: null
workflows:
  Workflow:
    name: Workflow
    jobs:
      "A":
        name: 'A'
        adaptor: '@openfn/language-common@latest'
        credential: null
        body: |
          fn(state => {
            // eee
            return state;
          })

    triggers:
      webhook:
        type: webhook
        enabled: true
    edges:
      webhook->A:
        source_trigger: webhook
        target_job: 'A'
        condition_type: always
        enabled: true
