name: joe-ghsync-v1
description: |
  Testing V1 GH sync

collections: null
channels: null
credentials: null
workflows:
  Workflow:
    name: Workflow
    jobs:
      "A":
        name: 'A'
        adaptor: '@openfn/language-image-utils@1.0.0'
        credential: null
        body: |
          // delete meeee
    triggers:
      webhook:
        type: webhook
        webhook_reply: before_start
        enabled: true
    edges:
      webhook->A:
        source_trigger: webhook
        target_job: 'A'
        condition_type: always
        enabled: true
